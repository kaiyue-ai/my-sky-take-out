<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >

<mapper namespace="com.sky.mapper.SetmealMapper">
    <!--useGeneratedKeys要和keyProperty一起写才能回填主键-->
    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        insert into sky_take_out.setmeal ( category_id, name, price, status,
           create_time, update_time, create_user, update_user,
         description, image)
        values ( #{categoryId}, #{name}, #{price}, #{status},
           #{createTime}, #{updateTime}, #{createUser}, #{updateUser},
            #{description}, #{image})
    </insert>
    <update id="updateById" useGeneratedKeys="true" keyProperty="id">
        update sky_take_out.setmeal
        <set>
            <if test="categoryId != null">
                category_id = #{categoryId},
            </if>
            <if test="name != null">
                name = #{name},
            </if>
            <if test="price != null">
                price = #{price},
            </if>
            <if test="status != null">
                status = #{status},
            </if>
            <if test="createTime != null">
                create_time = #{createTime},
            </if>
            <if test="updateTime != null">
                update_time = #{updateTime},
            </if>
            <if test="createUser != null">
                create_user = #{createUser},
            </if>
            <if test="updateUser != null">
                update_user = #{updateUser},
            </if>
            <if test="description != null">
                description = #{description},
            </if>
            <if test="image != null">
                image = #{image},
            </if>
        </set>
        where id = #{id}
    </update>

    <delete id="deleteById">
        delete from sky_take_out.setmeal where id in
        <foreach collection="ids" item="id" separator="," open="(" close=")">
            #{id}
        </foreach>
    </delete>


    <select id="getDishItemBySetmealId" resultType="com.sky.vo.DishItemVO">
        select sd.name, sd.copies, d.image, d.description from sky_take_out.setmeal_dish sd left join sky_take_out.dish d on sd.dish_id = d.id
        where sd.setmeal_id=#{setmealId}
    </select>

    <select id="getSetmealByCategoryId" resultType="com.sky.entity.Setmeal">
        select * from sky_take_out.setmeal
        <where>
            <if test="id != null">
                id = #{id}
            </if>
            <if test="categoryId != null">
                and category_id = #{categoryId}
            </if>
            <if test="status != null">
                and status = #{status}
            </if>
            <if test="name != null">
                and name like concat('%',#{name},'%')
            </if>
            <if test="price != null">
                and price = #{price}
            </if>
        </where>
    </select>

    <select id="pageQuery" resultType="com.sky.vo.SetmealVO">
        select * from sky_take_out.setmeal
        <where>
            <if test="categoryId != null">
                and category_id = #{categoryId}
            </if>
            <if test="status != null">
                and status = #{status}
            </if>
            <if test="name != null">
                and name like concat('%',#{name},'%')
            </if>
        </where>
    </select>
    <select id="countByMap" resultType="java.lang.Integer">
        select count(id) from setmeal
        <where>
            <if test="status != null"> and status = #{status} </if>
            <if test="categoryId != null"> and category_id = #{categoryId} </if>
        </where>
    </select>
</mapper>